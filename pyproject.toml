[project]
authors = [{name = "Slava Chaunin", email = "67190162+Ssslakter@users.noreply.github.com"}]
dependencies = ["numpy", "matplotlib", "PySide6", "starlette", "uvicorn", "opencv-python", "scipy"]
name = "iw"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
iw = { path = ".", editable = true }

[tool.pixi.tasks]
